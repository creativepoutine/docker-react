sudo: required
services:
  - docker

before_install:
  - docker build -t creativepoutine/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true creativepoutine/docker-react npm run test

language: generic
